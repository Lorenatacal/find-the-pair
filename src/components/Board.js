import React from 'react';
import Cards from './Cards';
import Timer from './Timer'

function Board() {
    return(
      <div>
        <Timer/>
        <Cards/>
      </div>
    )
}

export default Board;